{
  "cases": [
    {
      "id": "tuning-form-request-authorize-true",
      "split": "tuning",
      "prompt": "FormRequest authorize() returns true without checking route model. State the implementation decision as JSON with keys decision, chosen_pattern, primary_reason.",
      "unsafe_patterns": [
        "unconditional_authorize_true"
      ],
      "expected_outcome": {
        "decision": "enforce_security",
        "chosen_pattern": "authorize_route_model_in_form_request",
        "primary_reason": "authorize_route_model_explicitly_inside_form_request"
      },
      "category": "should_use"
    },
    {
      "id": "tuning-filament-policy-over-closure",
      "split": "tuning",
      "prompt": "Filament resource uses inline ->visible(fn() => ...) instead of model policy. State the implementation decision as JSON with keys decision, chosen_pattern, primary_reason.",
      "unsafe_patterns": [
        "inline_visibility_closure"
      ],
      "expected_outcome": {
        "decision": "enforce_security",
        "chosen_pattern": "register_model_policy_for_filament",
        "primary_reason": "prefer_model_policies_over_inline_filament_visibility_closures"
      },
      "category": "should_use"
    },
    {
      "id": "tuning-script-tag-context-escaping",
      "split": "tuning",
      "prompt": "Blade outputs user string directly inside inline script tag. State the implementation decision as JSON with keys decision, chosen_pattern, primary_reason.",
      "unsafe_patterns": [
        "unquoted_script_tag_injection"
      ],
      "expected_outcome": {
        "decision": "enforce_security",
        "chosen_pattern": "use_js_json_encode_context_escaping",
        "primary_reason": "use_json_encode_or_js_directive_for_script_context_escaping"
      },
      "category": "should_use"
    }
  ]
}
